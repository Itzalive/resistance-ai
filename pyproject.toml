[project]
name = "resistance-ai"
version = "0.1.3"
description = "Canonical resistance-engine skills library"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=9.0.2"]

[tool.pytest.ini_options]
addopts = ""
testpaths = ["tests"]
